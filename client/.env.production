# Used only by `npm run build` (production build). Left empty on purpose:
# the built frontend is served by the same Node server as the API (see
# server/src/index.js + root `npm start`), so API calls should be relative
# to whatever origin the page is loaded from, not a hardcoded localhost URL.
VITE_API_URL=
